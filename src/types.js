// @flow

export type User = {
  firstName: string,
  lastName: string,
  email: string,
  phoneNumber: string,
  discoveryMethod: string
};

export type Dance = {
  date: string,
  info: string
};

export type Checkin = {
  date: string,
  email: string
};
